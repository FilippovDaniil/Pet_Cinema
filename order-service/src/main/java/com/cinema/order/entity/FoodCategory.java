package com.cinema.order.entity;

// Категория позиции в меню кинотеатра.
// Хранится в БД как строка (EnumType.STRING в FoodItem) — безопасно при добавлении новых значений.
public enum FoodCategory {
    DRINK,    // Напитки: кола, вода, соки
    POPCORN,  // Попкорн (выделен отдельно — самая популярная позиция)
    SNACK,    // Снеки: начос, хот-дог, капкейк
    OTHER     // Прочее — зарезервировано для расширения меню
}
